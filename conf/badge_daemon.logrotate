/var/log/badge_daemon/badge_daemon.log {
	monthly
	missingok
	rotate 12
	compress
	delaycompress
	notifempty
	nocreate
	sharedscripts
	prerotate
		/bin/kill -s SIGUSR1 `cat /var/run/badge_daemon.pid`
	endscript
}
