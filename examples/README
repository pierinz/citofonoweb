In this folder are stored examples of configuration files.

LIGHTTPD:
- copy lighttpd-plain.user to /etc/lighttpd/
- copy lighttp.conf.example to /etc/lighttpd/lighttp.conf
- enable mod fastcgi-php and auth: lighttpd-enable-mod fastcgi-php auth
- /etc/init.d/lighttpd restart

You can use 'make lighttpd-config' to apply this configuration
WARNING: this will overwrite existing settings!!